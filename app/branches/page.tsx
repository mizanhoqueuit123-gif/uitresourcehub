import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
export default async function Branches(){const supabase=await createClient();const {data}=await supabase.from('departments').select('id,name,code,slug,description').order('name');return <div className="container py-12"><h1 className="text-4xl font-bold">Branches</h1><p className="mt-2 text-slate-500">Explore resources by engineering branch.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{(data??[]).map((b:any)=><Link key={b.id} href={`/branch/${b.slug}`} className="card p-6 hover:-translate-y-1"><span className="font-bold text-[#0b5ed7]">{b.code}</span><h2 className="mt-2 text-xl font-semibold">{b.name}</h2><p className="mt-2 text-sm text-slate-500">{b.description??'Browse academic resources for this branch.'}</p></Link>)}</div></div>}
